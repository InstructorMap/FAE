<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes">
    <title>FAE · Federación Argentina de Educación</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;700;900&display=swap" rel="stylesheet">
    <script>
        tailwind.config = { 
            theme: { 
                extend: { 
                    colors: { faeBlue: '#0f172a', faeGold: '#d97706', faeLight: '#f8fafc' }, 
                    fontFamily: { sans: ['Montserrat', 'sans-serif'] } 
                } 
            } 
        }
    </script>
    <style>
        body { background: #f8fafc; color: #0f172a; -webkit-tap-highlight-color: transparent; }
        .hero-pattern {
            background-color: #0f172a;
            background-image: radial-gradient(circle at 2px 2px, rgba(255,255,255,0.1) 1px, transparent 0);
            background-size: 32px 32px;
        }
        .card-registro {
            transition: all 0.3s ease;
        }
        .card-registro:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            border-color: #d97706;
        }
    </style>
</head>
<body class="min-h-screen flex flex-col">

    <div class="bg-faeGold text-black text-[10px] py-2 px-4 text-center font-bold tracking-widest uppercase z-50 relative">
        Acta N° 4651912 INPI · Personería Jurídica Nacional
    </div>

    <nav class="bg-white border-b border-slate-200 sticky top-0 z-40 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 h-20 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <div class="w-12 h-12 bg-faeBlue rounded-lg flex items-center justify-center shadow-inner">
                    <i data-lucide="book-open" class="w-7 h-7 text-faeGold"></i>
                </div>
                <div class="leading-tight">
                    <span class="font-black text-xl tracking-tight block text-faeBlue uppercase">F.A.E.</span>
                    <span class="text-[10px] text-slate-500 font-bold uppercase tracking-wider">Federación Argentina de Educación</span>
                </div>
            </div>
            
            <div class="hidden md:flex gap-6 text-xs font-bold text-slate-600 uppercase tracking-wider">
                <a href="#" class="hover:text-faeGold transition">Asociados</a>
                <a href="#" class="hover:text-faeGold transition">Avales</a>
                <a href="#" class="hover:text-faeGold transition">Institutos</a>
                <a href="#" class="hover:text-faeGold transition">Certificaciones</a>
            </div>

            <button class="md:hidden text-slate-800"><i data-lucide="menu"></i></button>
        </div>
    </nav>

    <header class="hero-pattern py-20 px-4 relative overflow-hidden flex flex-col items-center justify-center text-center">
        <div class="absolute inset-0 bg-gradient-to-b from-transparent to-faeBlue/90 z-0"></div>
        <div class="relative z-10 max-w-4xl mx-auto">
            <div class="inline-block mb-4 px-4 py-1.5 rounded-full bg-white/10 text-faeGold text-xs font-bold uppercase tracking-widest border border-white/20 backdrop-blur-md">
                Red Nacional de Registros
            </div>
            <h1 class="text-3xl md:text-5xl font-black mb-6 text-white leading-tight drop-shadow-lg">
                Validación Oficial de Competencias<br>
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-faeGold to-yellow-300">Académicas y Profesionales</span>
            </h1>
            <p class="text-slate-300 mb-10 text-sm md:text-base max-w-2xl mx-auto font-medium leading-relaxed">
                Plataforma centralizada de la Federación Argentina de Educación para la consulta pública y auditoría de Profesionales, Técnicos, Auxiliares, Instructores e Idóneos en todo el territorio nacional.
            </p>
        </div>
    </header>

    <main class="flex-grow py-16 bg-slate-50 relative -mt-6 rounded-t-3xl z-20">
        <div class="max-w-7xl mx-auto px-4">
            
            <div class="text-center mb-12">
                <h2 class="text-2xl font-black text-faeBlue uppercase tracking-tight">Ecosistema de Registros Oficiales</h2>
                <div class="w-16 h-1 bg-faeGold mx-auto mt-4 rounded-full"></div>
                <p class="text-slate-500 text-sm mt-4">Seleccione el área de competencia para acceder al padrón correspondiente.</p>
            </div>

            <div id="registros-container" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
                
                <div class="col-span-full text-center py-12 text-slate-400 animate-pulse">
                    <i data-lucide="loader-2" class="w-8 h-8 animate-spin mx-auto mb-2"></i>
                    <p class="text-xs font-bold uppercase tracking-widest">Sincronizando con la Red Federal...</p>
                </div>

            </div>

        </div>
    </main>

    <section class="py-16 bg-white border-t border-slate-100">
        <div class="max-w-6xl mx-auto px-4 grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
            <div>
                <div class="w-16 h-16 mx-auto bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center mb-4"><i data-lucide="briefcase" class="w-8 h-8"></i></div>
                <h4 class="font-black text-slate-800 uppercase text-sm">Bolsa de Trabajo</h4>
            </div>
            <div>
                <div class="w-16 h-16 mx-auto bg-emerald-50 text-emerald-600 rounded-2xl flex items-center justify-center mb-4"><i data-lucide="book-open-check" class="w-8 h-8"></i></div>
                <h4 class="font-black text-slate-800 uppercase text-sm">Capacitaciones</h4>
            </div>
            <div>
                <div class="w-16 h-16 mx-auto bg-purple-50 text-purple-600 rounded-2xl flex items-center justify-center mb-4"><i data-lucide="mic-2" class="w-8 h-8"></i></div>
                <h4 class="font-black text-slate-800 uppercase text-sm">Congresos</h4>
            </div>
            <div>
                <div class="w-16 h-16 mx-auto bg-orange-50 text-orange-600 rounded-2xl flex items-center justify-center mb-4"><i data-lucide="building" class="w-8 h-8"></i></div>
                <h4 class="font-black text-slate-800 uppercase text-sm">Consultoría</h4>
            </div>
        </div>
    </section>

    <footer class="bg-faeBlue text-slate-400 py-12 border-t border-slate-900 text-center">
        <div class="max-w-4xl mx-auto px-4">
            <div class="flex items-center justify-center gap-2 text-white mb-6">
                <i data-lucide="book-open" class="w-6 h-6 text-faeGold"></i>
                <span class="font-black text-xl tracking-tight uppercase">F.A.E.</span>
            </div>
            <div class="flex flex-wrap justify-center gap-4 text-[10px] uppercase font-bold tracking-widest text-slate-500 mb-8">
                <span>Profesionales</span> • <span>Técnicos</span> • <span>Auxiliares</span> • <span>Instructores</span> • <span>Idóneos</span>
            </div>
            <p class="text-xs text-slate-500 max-w-md mx-auto leading-relaxed mb-6">
                Institución reguladora y certificadora a nivel nacional. Todas las credenciales emitidas se encuentran aseguradas mediante encriptación y tecnología Blockchain.
            </p>
            <div class="text-[10px] text-slate-600 font-mono">
                Acta N° 4651912 INPI © 2026 · Desarrollado por ASARI Platform
            </div>
        </div>
    </footer>

    <script>
        // CONFIGURACIÓN SUPABASE
        const PROJECT_URL = 'https://vxggatcfrywyodovjwxj.supabase.co';
        const PUBLIC_KEY  = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZ4Z2dhdGNmcnl3eW9kb3Zqd3hqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjgyMzM5MjgsImV4cCI6MjA4MzgwOTkyOH0.RZ9jhEASO6nCV8EnzrHQBhglP4G1nik9Z5osPXysGNg';
        const db = supabase.createClient(PROJECT_URL, PUBLIC_KEY);

        lucide.createIcons();

        // FUNCIÓN PRINCIPAL: Cargar los Registros dinámicamente
        async function cargarEcosistema() {
            const container = document.getElementById('registros-container');
            
            try {
                // Traemos todas las instituciones activas ordenadas alfabéticamente
                const { data, error } = await db
                    .from('instituciones')
                    .select('*')
                    .eq('activo', true)
                    .order('nombre');

                if (error) throw error;

                if (data && data.length > 0) {
                    container.innerHTML = data.map(reg => {
                        // Color por defecto si no tiene
                        const colorTema = reg.color_tema || '#0f172a';
                        // Icono por defecto si no tiene logo
                        const logoHtml = reg.logo_url 
                            ? `<img src="${reg.logo_url}" alt="${reg.sigla}" class="w-full h-full object-contain p-2">`
                            : `<i data-lucide="shield-check" class="w-10 h-10" style="color: ${colorTema}"></i>`;

                        // AQUÍ ESTÁ LA MAGIA DE LA REDIRECCIÓN: registro.html?id=sigla
                        return `
                        <a href="registro.html?id=${reg.sigla}" class="card-registro bg-white border border-slate-200 rounded-2xl p-6 flex flex-col items-center text-center relative overflow-hidden group">
                            <div class="absolute top-0 left-0 w-full h-1" style="background-color: ${colorTema}"></div>
                            
                            <div class="w-20 h-20 rounded-xl bg-slate-50 border border-slate-100 mb-4 flex items-center justify-center shadow-sm">
                                ${logoHtml}
                            </div>
                            
                            <h3 class="font-black text-slate-800 text-sm mb-1 leading-tight group-hover:text-faeGold transition-colors">${reg.nombre}</h3>
                            <p class="text-[10px] font-mono text-slate-400 bg-slate-100 px-2 py-0.5 rounded uppercase tracking-wider mb-4 border border-slate-200">
                                ${reg.sigla}
                            </p>
                            
                            <div class="mt-auto w-full pt-4 border-t border-slate-100 text-xs font-bold flex justify-center items-center gap-2 transition-colors" style="color: ${colorTema}">
                                Consultar Padrón <i data-lucide="arrow-right" class="w-3 h-3"></i>
                            </div>
                        </a>
                        `;
                    }).join('');
                    
                    // Re-renderizar iconos de Lucide en el nuevo HTML
                    lucide.createIcons();
                } else {
                    container.innerHTML = `
                        <div class="col-span-full text-center py-12 bg-white rounded-2xl border border-slate-200 border-dashed">
                            <i data-lucide="folder-search" class="w-12 h-12 text-slate-300 mx-auto mb-3"></i>
                            <p class="text-slate-500 font-bold">No hay registros públicos configurados aún.</p>
                            <p class="text-xs text-slate-400 mt-1">Se pueden dar de alta desde el Panel de Administración FAE.</p>
                        </div>
                    `;
                    lucide.createIcons();
                }

            } catch (err) {
                console.error(err);
                container.innerHTML = `
                    <div class="col-span-full text-center py-12 text-red-500 bg-red-50 rounded-2xl border border-red-100">
                        <i data-lucide="alert-triangle" class="w-8 h-8 mx-auto mb-2"></i>
                        <p class="text-sm font-bold">Error de conexión con la red federal.</p>
                    </div>
                `;
                lucide.createIcons();
            }
        }

        // Iniciar
        document.addEventListener('DOMContentLoaded', () => {
            cargarEcosistema();
        });
    </script>
</body>
</html>
